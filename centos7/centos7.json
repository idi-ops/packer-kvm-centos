{
  "variables": {
    "base_name": "template-centos73-x86_64",
    "iso_name": "CentOS-7-x86_64-NetInstall-1611.iso",
    "iso_url": "http://centos.bhs.mirrors.ovh.net/ftp.centos.org/7.3.1611/isos/x86_64/CentOS-7-x86_64-NetInstall-1611.iso",
    "iso_checksum": "f2f7367deb90a25822947660c71638333ca0eceeabecc2d631be6cd508c24494",
    "iso_checksum_type": "sha256",
    "iso_path": "iso",
    "ssh_username": "root",
    "ssh_password": "root",
    "disk_size": "10240"
  },

  "provisioners": [
    {
      "type": "shell",
      "execute_command": "{{ .Vars }} sudo -E bash '{{ .Path }}'",
      "inline": [
        "yum -y install epel-release",
        "yum -y install ansible"
      ]
    },
    {
      "type": "ansible-local",
      "playbook_file": "ansible/playbook.yml",
      "playbook_dir": "ansible"
    },
    {
      "type": "shell",
      "execute_command": "{{ .Vars }} sudo -E bash '{{ .Path }}'",
      "inline": [
        "yum -y autoremove ansible"
      ]
    }
  ],

  "builders": [
    {
      "name": "{{ user `base_name` }}",
      "type": "qemu",
      "format": "qcow2",
      "accelerator": "kvm",
      "qemu_binary": "/usr/bin/qemu-kvm",
      "net_device": "virtio-net",
      "disk_interface": "virtio",
      "disk_cache": "none",

      "qemuargs": [[ "-m", "2048M" ],[ "-smp", "2" ]],

      "ssh_wait_timeout": "30m",

      "ssh_username": "{{ user `ssh_username` }}",
      "ssh_password": "{{ user `ssh_password` }}",
      "iso_urls": [
        "{{ user `iso_path` }}/{{ user `iso_name` }}",
        "{{ user `iso_url` }}"
      ],
      "iso_checksum": "{{ user `iso_checksum` }}",
      "iso_checksum_type": "{{ user `iso_checksum_type` }}",

      "boot_command": [
        "<tab> net.ifnames=0 biosdevname=0 text ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/ks.cfg<enter><wait>"
      ],

      "disk_size": "{{ user `disk_size` }}",

      "headless": "true",
      "http_directory": "http",
      "shutdown_command": "sudo /usr/sbin/shutdown -h now",
      "output_directory": "artifacts/qemu"
    }
  ]
}
